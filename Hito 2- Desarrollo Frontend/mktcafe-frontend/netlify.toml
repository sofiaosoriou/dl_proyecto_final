[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback: todas las rutas sirven index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
